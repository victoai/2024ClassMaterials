

마지막 최종본 채팅에 파일 보냈으니 
확인하고 싶은 분들은 참고하세요 ^ 

맛점하시고 오후에 실습시간 갖을께요 !


mvc 관리프로그램
등록
조회
변경
삭제

ok


세션사용하기
로그인
로그아웃
 

-----
코드번호 자동생성 
서버프로그램 실기 ( mvc) : 내부평가 

데이터베이스 연결후 시퀀스 만들기
create sequence  codeseq  start with  30;
select codesql.nextval  from  dual;



	     boolean result = (id.equals(pwd))?true: false;		
		if(result==true){	
			//회원인경우 세션에 id정보 저장
			HttpSession  session =request.getSession();
			session.setAttribute("id", id);	
			response.sendRedirect("/sessionTest/index");						
		}else{
			//회원아닌경우 , 로그인
			response.sendRedirect("/sessionTest/login");	
		} 
		


@WebServlet("/logout")
public class LogoutServlet extends HttpServlet {
 
	
	 @Override
	protected void doGet(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {
		 		 
		 HttpSession session=  request.getSession();
		 session.invalidate();
		 //
		 response.sendRedirect("/sessionTest/index");	 
		 
	}
	 
}