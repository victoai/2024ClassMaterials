 
//전체포인트합계
select  sum(m_point)
from member_tbl_11;

//고객등급별 포인트합계
select  sum(m_point)
from member_tbl_11
group by m_grade ;

//고객등급별 포인트합계를 고객등급과 함께 조회
select m_grade, sum(m_point)
from member_tbl_11
group by m_grade ;

//고객등급별 포인트합계 중에서
// 4000점이상인 경우만
select  sum(m_point)
from member_tbl_11
group by m_grade 
having sum(m_point) >=4000;



join--
tbl_test_order : 주문테이블
tbl_test_customer   :고객테이블

조인시 중심테이블 정하기

 select  *
 from  tbl_test_order ;


 select *
 from tbl_test_order   o
 join tbl_test_customer  c  
 on   o.id  = c.id  ;


 desc tbl_test_order  ;



select * from member_tbl_11 where m_point is not null;

select  m_grade ,  sum(m_point) from member_tbl_11 where m_point is not null group by  rollup(m_grade);

select  nvl(m_grade, '합계'),  sum(m_point) from member_tbl_11 where m_point is not null group by  rollup(m_grade);

select  nvl(m_grade, '합계'),  sum(m_point) from member_tbl_11 where m_point is not null group by  rollup(m_grade);
 
 






